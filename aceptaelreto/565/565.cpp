#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    while(scanf("%d", &n), n != 0){
        int t[n];
        int p[n];

        for (int i = 0; i < n; i++){
            scanf("%d %d", &t[i], &p[i]);
            // printf("%d %d\n", t[i], p[i]);
        }
       
    }

    return 0;
}
